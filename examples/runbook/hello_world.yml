name: local try
extension:
  paths:
    - "../testsuites"
environment:
  environments:
    - nodes:
        - type: local
platform:
  - type: ready
testcase:
  - criteria:
      area: demo
      category: simple
